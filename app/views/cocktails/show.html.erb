<div class="container">

  <h1> <%= @cocktail.name %> </h1>
    <ul>
      <% @doses.each do |dose| %>
         <li> <%= dose.description %> </li>
         <li> <%= dose.description.ingredient %> </li>
     <% end %>
    </ul>
    <div class="btn btn-primary">
    <%= link_to 'Back to cocktails', cocktails_path %>
    </div>
  </div>
